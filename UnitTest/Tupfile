
include_rules

LIBS += -lboost_unit_test_framework

: foreach *.cpp |> !cxx |>
: ./*.o $(SOURCE_DIR)/*.o $(DECISION_TREE_DIR)/*.o $(DUMPER_DIR)/*.o \
		$(STATUS_DIR)/*.o |> !linker |> unitTest
: unitTest |> !unitTest |>
